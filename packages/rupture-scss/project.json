{
  "name": "rupture-scss",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/rupture-scss",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Rupture is a pure SCSS library - no build step required'"
      }
    }
  },
  "tags": ["type:library", "scope:shared"]
}
