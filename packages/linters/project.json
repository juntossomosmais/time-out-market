{
  "name": "@juntossomosmais/linters",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/linters",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "npx shx cp -R ./packages/linters/src ./packages/linters/dist"
          }
        ]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "jest"
          }
        ]
      }
    },
    "test:ci": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "jest --coverage --ci --coverageDirectory=packages/linters/coverage"
          }
        ]
      }
    },
    "publish-package": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "npm publish --access public"
          }
        ]
      }
    }
  }
}
